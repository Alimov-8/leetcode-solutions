class Solution:
    def containsDuplicate(self, nums: List[int]) -> bool:
        counter = dict()
        for i in nums:
            try:
                counter[i] += 1
            except:
                counter[i] = 1
        d = {k:v for (k,v) in counter.items() if v > 1}
        return len(d) > 0
